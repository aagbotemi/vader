interface IMoviesForYouCard {
    _i: string
}

interface IRecommendationCard {
    _i: string;
}

interface IHero {
    learnMore?: boolean;
}

interface IMoviesForYou {
    heading: string
}

interface ICast {
    heading: string
}

export type {
    IMoviesForYouCard,
    IRecommendationCard,
    IHero,
    IMoviesForYou,
    ICast
};